export function useLogLogic({
    logData,
    setLogData,
    history,
    setHistory,
}) {

    const addSet = (ex) => {
        // 今のコードそのまま移動
    };

    const removeSet = (ex, idx) => {
        // 今のコードそのまま
    };

    const setField = (ex, idx, field, value) => {
        // そのまま
    };

    const saveLog = () => {
        // そのまま
    };

    return {
        addSet,
        removeSet,
        setField,
        saveLog,
    };
}